services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: cloudtypora
      POSTGRES_USER: cloudtypora
      POSTGRES_PASSWORD: cloudtypora
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  minio:
    image: minio/minio:RELEASE.2024-12-18T13-15-44Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: cloudtypora
      MINIO_ROOT_PASSWORD: cloudtypora-secret
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data

volumes:
  postgres-data:
  minio-data:
